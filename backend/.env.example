# Environment Variables for Canteen Backend

# MongoDB Connection String
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/canteenDb?retryWrites=true&w=majority

# JWT Secret for authentication
JWT_SECRET=your_super_secret_jwt_key_here

# Server Port (optional, defaults to 5000)
PORT=5000

# Node Environment
NODE_ENV=production
